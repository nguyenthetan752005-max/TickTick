<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions cho Reminder -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 31+ only -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
        tools:ignore="SuspiciousImport" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"
        tools:ignore="SuspiciousImport" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"
        tools:ignore="SuspiciousImport" />
    <!-- API 29+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"
        tools:ignore="SuspiciousImport" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Permissions cho Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.SEND_SMS"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />

    <!-- Permissions cho Media -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TickTick">
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.App.Starting"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity Danh bạ -->
        <activity
            android:name=".ContactsActivity"
            android:theme="@style/Theme.TickTick"
            android:exported="false" />

        <!-- Activity Media (Music) -->
        <activity
            android:name=".MediaActivity"
            android:theme="@style/Theme.TickTick"
            android:exported="false" />

        <!-- Activity Profile -->
        <activity
            android:name=".ProfileActivity"
            android:theme="@style/Theme.TickTick"
            android:exported="false" />

        <!-- Activity Custom Theme -->
        <activity
            android:name=".ui.theme.CustomThemeActivity"
            android:theme="@style/Theme.TickTick"
            android:exported="false" />

        <!-- Activity Cắt Ảnh uCrop -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>

        <!-- Foreground Service cho Reminder -->
        <service
            android:name=".service.ReminderService"
            android:exported="false"
            tools:ignore="SuspiciousImport" />

        <!-- Foreground Service cho Music Player -->
        <service
            android:name=".service.MusicPlayerService"
            android:exported="false" />

        <!-- Custom Content Provider cho phép app khác truy xuất Tasks -->
        <provider
            android:name=".provider.TaskContentProvider"
            android:authorities="hcmute.edu.vn.nguyenthetan.provider"
            android:exported="true" />

        <!-- BroadcastReceiver nhận alarm từ AlarmManager để hiện thông báo nhắc nhở -->
        <receiver
            android:name=".receiver.ReminderBroadcastReceiver"
            android:exported="false" />

        <!-- BroadcastReceiver lên lịch lại reminders sau khi thiết bị khởi động lại -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver nhận SMS đến và lưu vào DB -->
        <receiver
            android:name=".receiver.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>